[project]
name = "adb-tv-remote"
version = "0.1.0"
description = "Minimal Android TV remote over ADB (WiFi), works on PC and Pydroid"
authors = [{name = "DanVerse"}]
dependencies = ["customtkinter"]

[project.scripts]
adb-tv-remote = "remote:main"
